kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: drone-postgres
  namespace: gitops
  labels:
    app: postgres
spec:
  storageClassName: rook-ceph-block
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 30Gi
